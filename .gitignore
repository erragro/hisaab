# secrets — never commit
.env
.env.*
# Local AI Studio download used for comparison; it can contain browser config
# and a local Gemini key, so it must never be included in the repository.
aistudio-export/
!.env.example
*.pem
*-service-account*.json
serviceAccountKey.json

# python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# node / firebase emulator
node_modules/
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebase/

# os / editor
.DS_Store
.idea/
.vscode/
